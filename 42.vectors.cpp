#include <iostream>
#include <vector>
#include <algorithm>
#include <array>

int main()
{
    // std::vector<int> vector(10, 90); // 90
    std::vector<int> vector(10); // 0
    // int arr[] {4,3,5,7,58,8};
    // std::array<int, 5> arrt {4,2,6,7,3};

    // std::vector<int> vector2(10, 99);
    // std::vector<int> vector;

    // vector.clear();
    // vector.push_back(10); // 1
    // vector.push_back(2);  // 2
    // vector.push_back(4);  // 4
    // vector.push_back(54);  // 4
    // vector.push_back(5);  // 4
    // vector.push_back(90);  // 4

    // vector.push_back(4); // 4
    // vector.push_back(5); // 8

    // std::cout<< vector[0] << std::endl;
    // std::cout<< vector.at(0) << std::endl;

    // vector[0] = 90;
    // vector.front() = 90;
    // vector[2] = 90;
    // vector.back() = 12;

    // std::cout<< "The size is = " << vector.size() << std::endl;
    // std::cout<< "The capacity is = " << vector.capacity() << std::endl;

    // if (vector.empty())
    //     std::cout<< "Empty" << std::endl;
    // else std::cout<< "Not empty" << std::endl;

    // vector.assign({1,2,3,4,5,6,7,8});
    // vector.assign(arr, arr+6);
    // vector.assign(arrt.data(), arrt.data()+arrt.size());

    // for (auto &&i : vector)
    // {
    //     std::cout<< i << std::endl;
    // }

    // vector.clear(); // size = 0, capacity = capacity
    // vector.erase(vector.begin() + vector.size()-1);
    // vector.insert(vector.begin() + 2, 90);

    // std::cout<< "The size is = " << vector.size() << std::endl;
    // std::cout<< "The capacity is = " << vector.capacity() << std::endl;
    // vector.shrink_to_fit();
    // // vector.push_back(12);
    // std::cout<< "After" << std::endl;
    // std::cout<< "The size is = " << vector.size() << std::endl;
    // std::cout<< "The capacity is = " << vector.capacity() << std::endl;

    // for (auto value : vector)
    //     std::cout << value << std::endl;

    // std::cout<< "After" << std::endl;
    // std::cout<< "The size is = " << vector.size() << std::endl;
    // std::cout<< "The capacity is = " << vector.capacity() << std::endl;
    // std::cout << "Ends" << std::endl;

    // std::cout<< vector.max_size() << std::endl;
    // vector.reserve(123);

    // std::cout<< "The size is = " << vector.size() << std::endl;
    // vector.resize(10);
    // std::cout<< "After" << std::endl;
    // std::cout<< "The size is = " << vector.size() << std::endl;
    // std::cout<< vector.at(1) << std::endl;

    // std::cout << "*****vector\t";
    // for (auto value : vector)
    //     std::cout << value << ' ';

    // std::cout<< "\n";
    // std::cout << "*****vector2\t";
    // for (auto value : vector2)
    //     std::cout << value << ' ';
    // std::cout<< "\n";

    // vector2.swap(vector);

    // std::cout << "*****vector\t";
    // for (auto value : vector)
    //     std::cout << value << ' ';

    // std::cout<< "\n";
    // std::cout << "*****vector2\t";
    // for (auto value : vector2)
    //     std::cout << value << ' ';
    // std::cout<< "\n";

    // std::sort(vector.begin(), vector.end());
    // std::reverse(vector.begin(), vector.end());
    // for (auto value : vector)
    //     std::cout<< value << std::endl;
}
